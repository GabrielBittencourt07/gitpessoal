import pandas as pd 
import numpy as np 
from sklearn.ensemble import RandomForestClassifier
import matplotlib.pyplot as plt
import Tentativa_de_fazer_com_OO as mod

if __name__ == "__main__": 

    dogs = pd.read_csv("df_dogs.csv")
    raw = pd.read_csv("df_raw.csv")

    dataset_teste = mod.DadosIMU(raw).dados_teste()
    dataset_menor = mod.DadosIMU(raw).dados_filt()
    
    dataset_teste.to_csv("df_filt.csv", index = False)
    dataset_teste.to_csv("df_test.csv", index = False)

    

